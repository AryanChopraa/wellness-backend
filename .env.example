# Server
PORT=4000

# CORS: leave unset to allow all origins (dev), or set to comma-separated origins (e.g. https://app.example.com,https://www.example.com)
# CORS_ORIGIN=http://localhost:3000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/wellness-platform

# JWT (use a strong secret in production)
JWT_SECRET=your-secret-change-in-production
JWT_EXPIRES_IN=7d

# OTP
OTP_EXPIRY_MINUTES=10

# Optional: set to production to disable dev OTP route and console OTP logging
# NODE_ENV=production

# Venice AI (actual chat replies)
VENICE_API_KEY=your-venice-inference-key
# VENICE_CHAT_MODEL=venice-uncensored

# OpenAI (conversation title generation only)
OPENAI_API_KEY=your-openai-api-key
# OPENAI_TITLE_MODEL=gpt-4.1-nano
